const checker = (num) => {

if (num>0) //if the value is greater than zero'
{
	console.log(1);
}
else if (num<0) //if less than zero'
{
	console.log(-1);
}
else
{
	console.log(0);//if equals zero'
}
return num;
}
checker(-2)
